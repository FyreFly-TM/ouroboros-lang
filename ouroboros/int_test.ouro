// Test file for int variables

function main() {
    // Regular variable declaration (existing syntax)
    let x = 10;
    print("Regular variable x = " + to_string(x));
    
    // Testing int function
    let result = square(5);
    print("Square of 5 = " + to_string(result));
    
    return 0;
}

function square(n) {
    return n * n;
} 